// Auth is handled via the users table in the database
// Use the CRUD functions in lib/crud/users.ts for authentication operations
// Better Auth integration can be added later if needed

export { }
