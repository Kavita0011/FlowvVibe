{
  "name": "flowvibe",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "cd frontend && npm ci && npm run build",
    "deploy": "npm run build && cd backend && npx wrangler deploy"
  }
}
